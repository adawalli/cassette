{
  "name": "@cassette-meetings/cli",
  "version": "0.2.2",
  "description": "CLI tool that watches a directory for meeting transcripts and produces clean Markdown summaries using an LLM",
  "module": "dist/index.js",
  "type": "module",
  "license": "MIT",
  "author": "cassette-meetings",
  "keywords": [
    "meeting",
    "transcript",
    "llm",
    "cli",
    "vtt",
    "openai"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adawalli/cassette.git"
  },
  "homepage": "https://github.com/adawalli/cassette#readme",
  "bugs": {
    "url": "https://github.com/adawalli/cassette/issues"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "bin": {
    "cassette": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "bun build index.ts --outdir dist --target bun --banner '#!/usr/bin/env bun' && chmod +x dist/index.js",
    "prepare": "husky || true",
    "prepublishOnly": "bun test && bun run build"
  },
  "lint-staged": {
    "*.{json,md,yaml,yml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "jsonpath-plus": "^10.4.0",
    "openai": "^6.22.0",
    "p-retry": "^7.1.1",
    "picomatch": "^4.0.3",
    "zod": "^4.3.6"
  }
}
